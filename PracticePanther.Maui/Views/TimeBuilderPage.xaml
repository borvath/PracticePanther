<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:PracticePanther.Maui.ViewModels"
             x:Class="PracticePanther.Maui.Views.TimeBuilderPage">
    <ContentPage.BindingContext>
        <viewmodel:TimeBuilderViewModel/>
    </ContentPage.BindingContext>
    
    <ScrollView StyleClass="page">
        <VerticalStackLayout Margin="20, 0, 20, 0" 
                             Spacing="0" 
                             VerticalOptions="Center"
                             WidthRequest="450"
                             BackgroundColor="#a9a9a9">
            <Label FontSize="48" HorizontalTextAlignment="Center" TextColor="#151515">Add Time Entry</Label>
            <Frame StyleClass="frame-entry">
                <HorizontalStackLayout StyleClass="add-entry" Spacing="20">
                    <HorizontalStackLayout>
                        <!-- Fix Picker Binding -->
                        <Picker Title="Select a client"
                                WidthRequest="150"
                                StyleClass="entry-field">
                        </Picker>
                    </HorizontalStackLayout>
                    <HorizontalStackLayout>
                        <Label Text="Employee ID: "
                               VerticalOptions="Center" 
                               WidthRequest="90"
                               StyleClass="entry-label"/>
                        <Entry Text="{Binding EmployeeId, Mode=TwoWay}"
                               Placeholder="0"
                               VerticalOptions="Center" 
                               WidthRequest="100"
                               StyleClass="entry-field"/>
                    </HorizontalStackLayout>
                </HorizontalStackLayout>
            </Frame>
            <Frame StyleClass="frame-entry">
                <HorizontalStackLayout StyleClass="add-entry" Spacing="20">
                    <HorizontalStackLayout>
                        <Label Text="Hours: " 
                               VerticalOptions="Center" 
                               WidthRequest="70"
                               StyleClass="entry-label"/>
                        <Entry Text="{Binding Hours, Mode=TwoWay}"
                               Placeholder="20.0"
                               Keyboard="Numeric"
                               VerticalOptions="Center"
                               WidthRequest="100"
                               StyleClass="entry-field"/>
                    </HorizontalStackLayout>
                    <HorizontalStackLayout>
                        <Label Text="Date: " 
                               VerticalOptions="Center" 
                               WidthRequest="70"
                               StyleClass="entry-label"/>
                        <DatePicker Date="{Binding Date}"
                                    VerticalOptions="Center"
                                    WidthRequest="120"
                                    StyleClass="entry-field"/>
                    </HorizontalStackLayout>
                </HorizontalStackLayout>
            </Frame>
            <Frame StyleClass="frame-entry">
                <HorizontalStackLayout StyleClass="add-entry">
                    <Label Text="Summary: "
                           VerticalOptions="Start"
                           WidthRequest="70"
                           StyleClass="entry-label"/>
                    <Editor Text="{Binding Narrative}"
                            Placeholder="Reason for entry..."
                            WidthRequest="250"
                            HeightRequest="250"
                            StyleClass="entry-field"/>
                </HorizontalStackLayout>
            </Frame>
            <Frame BackgroundColor="Transparent">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Column="0" Grid.Row="0"
                            Clicked="AddClicked"
                            BackgroundColor="#006699"
                            FontSize="24"
                            Text="Confirm"/>
                </Grid>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>